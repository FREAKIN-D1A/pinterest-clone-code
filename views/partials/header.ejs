<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%=title%></title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.1.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full px-10 py-3 flex justify-between bg-zinc-800 text-white">
      <h3 class="text-3xl">Pinterest Clone</h3>
      <div class="flex gap-10 items-center">
        <a href="/profile">Profile</a>
        <a href="/feed">Feed</a>

<% if (isLoggedIn)  {  %>
  <a href="/logout" class="px-3 py-2 bg-red-600 text-white rounded-full">Log out</a>
<% }  else { %>
<a href="/" class="px-3 py-2 bg-red-600 text-white rounded-full">Log in</a>
<a href="/register" class="px-3 py-2 bg-red-600 text-white rounded-full">Sign Up</a>
<% }  %>
        
      </div>
    </div>
    